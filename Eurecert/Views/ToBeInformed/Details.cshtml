@model Eurecert.Models.ToBeInformed

@{
    ViewData["Title"] = "Details";
}

<h2>Detaylar</h2>

<div>
    <h4>Haberdar Olma Şekli</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.BeInformed)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.BeInformed)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model.Id">Düzenle</a> |
    <a asp-action="Index">Geri</a>
</div>
